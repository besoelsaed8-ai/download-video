# دليل النشر — ارفع الموقع مجاناً

## الخيار 1 (مُوصى به): Render — خادم كامل، مجاني، 3 خطوات

المزايا: كل المنصات تعمل (TikTok بدون علامة + إنستغرام + يوتيوب وكل المواقع بالدمج) + الوسيط الآمن + SEO تلقائي.

1. أنشئ حساباً مجانياً في https://render.com (ببريدك أو GitHub).
2. ارفع الكود إلى GitHub:
   ```bash
   git init
   git add -A
   git commit -m "Download Video"
   # أنشئ مستودعاً جديداً على github.com ثم:
   git remote add origin https://github.com/اسمك/download-video.git
   git push -u origin main
   ```
3. في Render: **New +** → **Blueprint** → اختر المستودع. سيقرأ `render.yaml` وينشر تلقائياً.
   بعد أول نشر: افتح الخدمة → **Environment** وضع `SITE_URL` = نطاقك الحقيقي (مهم للـ SEO).

ملاحظات:
- النسخة المجانية تنام بعد 15 دقيقة بدون زيارات وتستيقظ خلال ~30 ثانية (طبيعي).
- دمج جودة يوتيوب العالية يحتاج ffmpeg على الخادم (اختياري لاحقاً)؛ بدونها تظهر رسالة واضحة ويبقى الباقي يعمل.
- إنستغرام من خوادم Render قد يُحجب: أضف `IG_SESSIONID` من حسابك إن أردت إنستغرام قوياً.

## الخيار 2: استضافة ثابتة فقط (Netlify Drop / GitHub Pages) — أسرع لكن محدودة

تعمل: روابط **تيك توك** مباشرة من المتصفح. لا تعمل: إنستغرام والمواقع الأخرى والدمج والوسيط.

1. جهّز المجلد مع استبدال النطاق:
   ```bash
   SITE_URL=https://your-domain.com/ node scripts/prepare-static.mjs
   ```
2. اذهب إلى https://app.netlify.com/drop واسحب مجلد `dist/` إلى الصفحة — يظهر موقعك فوراً.
3. غيّر اسم النطاق الفرعي من الإعدادات (Settings → Domain).

## بعد النشر على أي خيار

- ضع `SITE_URL` بالنطاق النهائي.
- سجّل في **Google Search Console** وارفع `sitemap.xml`.
- ارفع `ads.txt` بمعرّف ناشر AdSense إلى جذر النطاق بعد تفعيل الحساب.
